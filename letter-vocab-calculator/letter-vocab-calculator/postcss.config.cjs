/**
 * PostCSS configuration used by Vite.  It invokes TailwindCSS and
 * Autoprefixer so that all generated styles are prefixed for browser
 * compatibility.
 */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}
  }
};